[pytest]
asyncio_mode = auto
pythonpath = src
testpaths = tests
addopts = -ra --strict-markers --tb=short
markers =
    integration: requires postgres+redis+evolution_api running
    slow: > 1s per test
    harness: harness scenarios (may require LLM)
